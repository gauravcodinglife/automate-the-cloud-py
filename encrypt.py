# from pyDes import des, ECB, PAD_PKCS5

# key = b"DESKey12"  # 8 bytes
# data = b"GauravDESDemo"
# cipher = des(key, ECB, padmode=PAD_PKCS5)
# encrypted = cipher.encrypt(data)
# print(encrypted.hex())
